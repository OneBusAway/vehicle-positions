<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only: permits cleartext (HTTP, non-TLS) traffic to the Android emulator's host-loopback
  alias and localhost, so the driver app can be pointed at a local dev server
  (`make up`, http://10.0.2.2:8080) during manual testing. Never merged into release builds —
  this file lives under src/debug, not src/main.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
